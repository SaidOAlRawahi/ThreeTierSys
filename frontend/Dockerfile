# Use a lightweight web server as the base image
FROM nginx:alpine

# Copy the HTML file and related assets to the web server's root directory
COPY index.html /usr/share/nginx/html/

# Expose port 80 for HTTP access
EXPOSE 80
